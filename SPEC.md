# 虚拟女友陪伴聊天 Web - 项目 SPEC

**版本**:v1.0
**日期**:2026-05-05
**目标周期**:3 天 MVP

---

## 1. Problem Statement(问题陈述)

我正在跟随一门"Web AI 出海"课程学习,需要一个练手项目来串联以下技术栈,完成从 0 到 1 的端到端实操:

- Next.js 14(App Router)前端框架
- BetterAuth 第三方登录系统
- Neon Serverless Postgres 数据库
- Drizzle ORM 数据访问层
- DeepSeek LLM API(主对话 + 信息抽取)
- Vercel Serverless 部署 + Namecheap 自定义域名
- Cloudflare R2 对象存储(本期暂不接入,预留接口)
- Postman API 调试

这是一个**纯技术练手项目**,不追求商业可行性、用户增长或市场验证。预期使用范围为我本人和 5-20 位朋友的小范围试用。

直接选择"虚拟女友陪伴聊天"作为业务场景,是因为它能**自然覆盖**上述所有技术点,且包含足够的工程复杂度(状态管理、记忆系统、流式响应、复杂数据建模)能起到练手效果。

---

## 2. Proposed Solution(方案描述)

### 2.1 产品定位

一个面向少量熟人用户的虚拟陪伴聊天 Web 应用,核心体验是**情感陪伴 + 角色扮演沉浸**:

- 用户在 4 个性格鲜明的预设角色中选择陪伴对象
- 通过持续聊天建立可发展的"关系",好感度从陌生升至恋人,共 5 个阶段
- AI 通过对话**逐步认识**用户(生日、爱好、职业等),并在后续对话中体现"她记得你"
- 4 个角色具有**显著差异化**的人设、说话风格和情绪反应模式

### 2.2 4 个预设角色

| 角色 ID | 名字 | 标签 | 性格关键词 |
|---------|------|------|------------|
| `sister` | 林晚(28岁) | 邻家姐姐 | 温柔细心、爱操心、内敛深情 |
| `cute` | 桃桃(20岁) | 可爱学妹 | 活泼黏人、玻璃心、占有欲强 |
| `cool` | 沈知夏(32岁) | 高冷御姐 | 独立疏离、嘴硬心软、克制深情 |
| `teacher` | 方雨檬(26岁) | 泼辣老师 | 火爆直接、刀子嘴豆腐心、护短 |

每个角色拥有约 1500-2000 字的完整 systemPrompt,定义其背景、性格、口头禅、说话风格、情绪触发条件、表达爱意的方式。

### 2.3 核心功能模块

**M1:认证与用户系统**
- BetterAuth 接入 Google OAuth 一键登录
- 首次登录后强制 onboarding 引导,要求用户填写"希望被怎么称呼"的昵称
- 后续登录跳过 onboarding,直接进入角色选择页

**M2:角色选择**
- 网格布局展示 4 张角色卡(选择页大图 + 名字 + 描述 + 当前好感度 + 当前阶段 Badge)
- 当前用户与每个角色的关系状态从数据库实时读取
- 点击卡片跳转聊天页

**M3:主对话(产品核心)**
- 流式响应(Vercel AI SDK + DeepSeek API)
- 消息历史完整保存,支持滚动到顶部加载更早消息(游标分页,每次 30 条)
- 单条消息长度限制 500 字
- AI 主动开场,根据距上次进入时间不同,生成 4 种不同开场白(首次 / 同天 / 隔天 / 久违)

**M4:用户画像记忆系统**
- 短期记忆:每次调用 LLM 时,注入最近 15 条对话历史
- 长期记忆:每次用户发送消息后,**异步调用** DeepSeek 抽取个人信息(生日、爱好、职业、喜欢的食物等),upsert 到 `user_profiles` 表
- 长期记忆**全局共享**:4 个角色读取同一份用户画像
- 每次对话时,用户画像作为 prompt 的一部分注入,实现"她记得我"

**M5:好感度与情绪系统**
- 5 阶段:陌生(0-20)→ 认识(21-40)→ 朋友(41-60)→ 暧昧(61-85)→ 恋人(86-100)
- 初始好感度:10
- **加分规则**:每条消息 +2(基础),情感正向 +3(LLM 异步判断),分享个人信息 +5,连续 N 天聊天 +5。每日加分上限 +50
- **减分规则**:24 小时未聊天 -3,72 小时未聊天额外 -10,冷淡敷衍 -2(LLM 判断),辱骂 -10,提到其他角色名字 -5。每日减分上限 -30
- 阶段跃迁影响:对用户的称呼、回复语气、AI 主动发图频率
- **节奏目标**:用户正常聊天 2 天可达"恋人"阶段
- 情感判断 + 信息抽取**合并为一次** LLM 调用,降低成本

**M6:角色主动发图(预生成图集方案)**
- 每个角色提前准备 8-10 张不同表情/场景的图片,统一风格预生成
- 静态存放于 `public/characters/{id}/gallery/` 目录
- AI 回复时,根据当前好感度阶段按概率触发发图(陌生 0% / 认识 5% / 朋友 15% / 暧昧 30% / 恋人 40%)
- 每个角色每用户每天最多发 3 张,从图集中随机选取
- 数据库 `messages.imageUrl` 字段存储图片路径
- **预留升级路径**:二期可将随机选图替换为图生图 API(如 nano-banana-2 / seedream-5),前端 UI 不需修改

### 2.4 视觉设计

暗色系氛围风:
- 背景主色 zinc-950,卡片背景 zinc-900
- 主强调色琥珀(amber-400),恋人阶段切换为玫红(rose-400)
- 用户消息气泡(琥珀)右对齐,AI 消息气泡(zinc-800)左对齐
- shadcn/ui 组件库(Slate 主题微调至 zinc)

### 2.5 部署策略

- **Day 1 收尾**:首次部署到 Vercel,使用默认 `.vercel.app` 域名,验证完整链路
- **Day 3 收尾**:Namecheap 购买域名 + Vercel 绑定,正式上线

---

## 3. Technical Constraints(技术约束)

### 3.1 强制技术栈

| 类别 | 选型 | 备注 |
|------|------|------|
| 前端框架 | Next.js 14 App Router | 课程要求 |
| 语言 | TypeScript | |
| 样式 | Tailwind CSS + shadcn/ui | |
| 数据库 | **仅 Neon Serverless Postgres** | 不引入其他数据库(包括 Redis) |
| ORM | Drizzle ORM + Drizzle Kit | |
| 认证 | BetterAuth + Google OAuth Provider | |
| LLM | **仅 DeepSeek**(`deepseek-chat` 模型) | 通过 OpenAI 兼容模式调用 |
| AI SDK | Vercel AI SDK | 用于流式响应封装 |
| 部署 | Vercel | |
| 域名 | Namecheap | |
| 包管理 | pnpm | |
| 代码托管 | GitHub | |

### 3.2 约束与边界

- **不引入 Redis 或其他缓存层**(用户只学过 Neon)
- **不引入 Cloudflare R2**(本期使用 public 目录静态资源)
- **不引入图生图 API**(本期使用预生成图集)
- **不引入邮件服务 / Cron Job**(本期不做主动联系)
- **熟人圈使用,不做严格内容审核**:仅有 500 字消息长度限制,无频率限制、无关键词过滤
- **不做开发者模式**:调试通过 Drizzle Studio 或 Neon SQL Editor 直接修改数据库
- **不做 Vercel Cron 任务**:每日加分/减分上限重置通过"用户访问时检查日期"的惰性方式实现

### 3.3 性能与成本约束

- 主对话必须流式响应,首字节延迟 < 1.5 秒
- 信息抽取与情感判断**必须异步**,不阻塞主对话返回
- 信息抽取与情感判断**合并为单次 LLM 调用**,以降低成本
- 整个项目所有用户的 LLM 调用总成本上限:**¥20**

---

## 4. Non-Goals(明确不做的事情)

以下功能在 v1.0 MVP 中**明确不做**,不投入任何开发时间:

### 4.1 砍掉的功能

| 功能 | 原因 |
|------|------|
| ❌ 真正的图生图(nano-banana / seedream-5 集成) | 工作量超出 3 天 MVP,用预生成图集替代 |
| ❌ Cloudflare R2 集成 | 本期图片用 public 静态资源 |
| ❌ TTS 语音回复 | 单独需要 0.5-1 天调试,移至二期 |
| ❌ Cron Job 定时任务 | 移至二期,作为独立练习项目 |
| ❌ 邮件提醒(Resend 集成) | 域名 DNS 验证 + 模板调试时间过长 |
| ❌ AI 主动联系用户(久违未登录推送) | 通过登录时开场白补偿,不做真主动推送 |
| ❌ 分享功能(分享聊天截图、角色卡) | 移至二期 |
| ❌ 隐私模式 / 访客密码 | 熟人圈不需要 |
| ❌ 导出聊天记录 | 移至二期 |
| ❌ 删除账号 / 数据清理流程 | 熟人圈不需要,需要时直接改数据库 |
| ❌ 节日特殊事件(生日祝福、情人节剧情) | 依赖 Cron,移至二期 |
| ❌ 频率限制(每分钟消息数限制) | 熟人圈风险低,500 字长度限制已够 |
| ❌ 开发者模式 / 后台调试页 | 直接用 Drizzle Studio |
| ❌ 邮箱密码登录 / 多种第三方登录 | 仅 Google OAuth |
| ❌ 用户自定义角色 / 角色编辑器 | 仅 4 个预设角色 |
| ❌ 跨角色独立记忆 | 用户画像全局共享 |

### 4.2 不追求的目标

- **不追求**用户留存或商业化:这是练手项目,做完即止
- **不追求**高并发支撑:5-20 人熟人圈使用,Neon 免费额度足够
- **不追求**移动端 App:仅 Web 响应式,适配桌面优先
- **不追求**多语言:仅中文
- **不追求**极致 UI 设计:够用、暗色系、克制即可
- **不追求**单元测试 / E2E 测试覆盖率:手动测试为主

---

## 5. Success Criteria(成功标准)

项目完成需满足以下**全部 9 项必达标准**,加分项可选:

### 5.1 必达标准(Must-Have)

| 编号 | 标准 | 验证方式 |
|------|------|----------|
| **S1** | 整套技术栈端到端跑通(Next.js + Neon + Drizzle + BetterAuth + DeepSeek + Vercel) | 各模块均能正常运行,无报错卡死 |
| **S2** | 代码部署到 Vercel + 绑定 Namecheap 真实域名,任何人能通过域名访问 | 浏览器输入域名能看到登录页 |
| **S3** | 代码推到 GitHub,有完整 README 文档(架构图、技术栈说明、踩坑记录) | GitHub 仓库可访问,README 完整 |
| **S4** | 所有 P0 功能 100% 完成 | 见下方 P0 清单 |
| **S5** | 我自己用 1 小时内能聊到"暧昧"或"恋人"阶段,体验完整关系发展 | 实测,数据库中 affection ≥ 61 |
| **S6** | 4 个角色在同一段对话里有明显不同的回复风格,测试人能分辨 | 找朋友盲测 |
| **S7** | AI 在第二天进入聊天时,确实记得用户之前说过的事(测试生日记忆) | 实测一次完整流程 |
| **S8** | 历史聊天能正确加载,刷新不丢,滚动分页正常 | 累计 50+ 条消息后实测 |
| **S9** | 朋友试用过程中没出严重 bug(白屏、无限报错、登不上) | 邀请 ≥ 3 朋友试用,无 P0 级 bug |

### 5.2 加分项(Nice-to-Have)

| 编号 | 标准 |
|------|------|
| S10 | 至少 3 个朋友试用反馈"还挺有意思的" |
| S11 | 写完后能向别人讲清楚每个技术点的作用(架构图、数据流、关键决策) |

### 5.3 P0 功能清单(对应 S4)

- [ ] Google 登录 + 自定义昵称 onboarding
- [ ] 4 个角色 + 选择页(展示当前好感度和阶段)
- [ ] 主对话(流式响应)+ 消息历史持久化
- [ ] 好感度系统(基础规则 + 5 阶段)
- [ ] 用户画像异步抽取 + 注入 prompt

### 5.4 P1 功能清单(尽量做)

- [ ] 历史滚动分页加载
- [ ] 4 种开场白(首次 / 同天 / 隔天 / 久违)
- [ ] AI 主动发图(预生成图集 + 概率触发)

### 5.5 P2 功能清单(锦上添花)

- [ ] 好感度变化的 UI 反馈动画(+2 浮起等)
- [ ] 阶段跃迁的特效提示

---

## 6. 项目时间表

| 时间 | 主要任务 |
|------|---------|
| **Day 1 上午** | 项目初始化、数据库 schema、BetterAuth 接入、登录页、角色选择页 |
| **Day 1 下午** | 主对话流程(流式响应、消息存储、加载历史)、4 个角色 prompt 写入数据库 |
| **Day 1 晚** | 首次部署到 Vercel(.vercel.app 域名),验证完整链路 |
| **Day 2 上午** | 用户画像抽取系统、好感度规则引擎、prompt 动态注入 |
| **Day 2 下午** | 4 种开场白、滚动分页、UI 打磨 |
| **Day 2 晚** | 准备 4 个角色的图片资源(选择页大图 + 头像 + 8-10 张图集) |
| **Day 3 上午** | AI 主动发图逻辑、好感度 UI 反馈 |
| **Day 3 下午** | 整体打磨、bug 修复、Namecheap 域名购买 + 绑定 |
| **Day 3 晚** | README 撰写、最终验收、邀请朋友试用 |

---

## 7. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| Google OAuth 配置踩坑 | 登录功能延期 | 提前在 Google Console 配置,本地 + 生产回调 URL 都加 |
| Drizzle 不熟导致开发效率低 | 整体进度滞后 | 项目正式开始前花 20 分钟过一遍 Drizzle 基础 |
| 4 张角色图片风格不统一 | 视觉效果打折 | 用同一个 AI 工具同一组 prompt 风格生成 |
| DeepSeek API 不稳定 | 对话功能异常 | 预留 OpenRouter 作备选,代码中 baseURL 可切换 |
| Vercel 部署环境变量遗漏 | 生产环境无法运行 | 维护一份 `.env.local.example`,部署前 checklist 对照 |
| 时间不够,P1 功能做不完 | MVP 完整度受影响 | 严格保 P0,P1 顺序按 4 种开场白 → 滚动分页 → 主动发图,按时间砍尾部 |

---

## 8. 数据库设计

### 8.1 表结构概览

| 表名 | 用途 | 备注 |
|------|------|------|
| `users` | 用户基本信息 | BetterAuth 自动生成 |
| `accounts` | 第三方登录关联 | BetterAuth 自动生成 |
| `sessions` | 登录会话 | BetterAuth 自动生成 |
| `characters` | 4 个预设角色 | 种子数据写入 |
| `user_character_relations` | 用户与角色的关系状态 | 包含好感度、阶段、计数器 |
| `messages` | 聊天消息记录 | 包含文字与图片 |
| `user_profiles` | 用户画像(全局共享) | 一对一关联 users |

### 8.2 关键字段说明

**`user_character_relations`**
- `affection` - 当前好感度数值 0-100
- `stage` - 当前阶段(stranger / acquaintance / friend / intimate / lover)
- `lastChatAt` - 最近一次聊天时间(用于超时减分与开场白判断)
- `consecutiveDays` - 连续聊天天数
- `todayAddition` / `todayDeduction` - 今日加分/减分累计
- `countersResetDate` - 每日计数器重置日期
- 复合唯一索引 `(userId, characterId)`

**`messages`**
- `role` - 'user' 或 'assistant'
- `content` - 消息文本内容
- `imageUrl` - 图片路径(默认 null)
- `affectionDelta` - 该消息引起的好感度变化
- `affectionAfter` - 变化后的好感度数值
- 索引 `(userId, characterId, createdAt DESC)` 加速历史查询

**`user_profiles`**
- `nickname` - 用户希望被称呼的名字(onboarding 时填写)
- `birthday` / `hobbies` / `favoriteFood` / `job` - LLM 抽取的画像信息
- `otherInfo` - JSONB,扩展字段

---

## 9. 角色 Prompt 结构

每个角色的 systemPrompt 包含以下结构(详细内容已单独文档化):

1. **基本信息**:年龄、职业、居住、家人/宠物
2. **成长背景**:出生地、家庭、教育、过往经历
3. **性格内核**:核心人格特质 4-6 条
4. **口头禅**:5-7 个标志性表达
5. **说话风格**:语气词使用、句长、是否用网络梗
6. **关心用户的触发情境**:3 类典型场景及反应
7. **表现情绪的触发情境**:冷淡/吃醋/被夸赞等场景
8. **怎么让用户离不开**:角色特有的关系绑定策略
9. **说"我爱你"的条件**:好感度阈值 + 触发情境
10. **绝对不做**:守护人设的禁区

每次 LLM 调用时,在 systemPrompt 末尾动态注入:
- 当前好感度数值与阶段
- 对用户的称呼方式
- 回复语气要求
- 用户画像信息
- 最近 15 条对话历史

---

## 文档结束

**下一步行动**:
1. (可选)花 20 分钟过一遍 Drizzle 基础
2. 按 Day 1 上午初始化清单开始开发
3. 每完成一个模块,对照 SPEC 验收
